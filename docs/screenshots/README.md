# Screenshots

Drop these PNGs here (referenced by the root README). Recommended ~1600px wide, compressed:

- `planner.png` — /plan with courses across terms, a validation badge visible
- `chat.png` — /chat mid-conversation showing citation chips
- `map.png` — /map prerequisite DAG
- `course-detail.png` — /courses/COMP%20472 showing difficulty votes + reviews + Reddit summary
- `dashboard.png` — /dashboard with AI insight + recommendations widgets
- `public-profile.png` — /u/<slug> public profile

Capture in light mode at a clean window size; optionally a dark-mode variant.
